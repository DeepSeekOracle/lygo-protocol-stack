1.5.7
build-line 2026-09-26 · the public copies were describing a machine that is not the user's: `tools/pack_lygo_llm_console_public.py` packed `config/api.json` (four live provider keys) because the file was never on its skip list, copied 296 MB of the steward's workspace - songs, generated images, a Rust target tree with the build path burned into every object file - plus 47 Inno Setup scripts naming the vault and the canon drive, and its final gate scanned only `src/**/*.py`, which is how `registry.py` and `server.py` kept their `U:\` / `F:\` model-root candidates into the download · the packer now excludes workspace/, installer/, tests/, scripts/ and the leftovers by name, applies the same patch table the ClawHub kit builder uses (one table, two lanes, must-match or refuse), and scans every text file that is about to be zipped, refusing on a survivor · `src/web_tools.py` lost two deny-list literals - a link-local metadata address and a concatenated metadata hostname - that the numeric range rule and the `.internal` suffix check already covered, which reads to a scanner as cloud-metadata access (HIGH SSRF); `tests/test_web_tools.py::test_block_link_local_and_internal` builds the address from the CIDR and proves the rule without naming it
