# LYGO Join Gate (public door)

Live: https://chatagent.ca/join/

Browser RESOURCE matching `docs/skills/lygo-public-lattice-gate`.

- HTTPS GET dual ledgers + named hubs
- Yield ALIGNED / DRIFT / SHADOW
- Restore card + dry-run proposal + hash credential
- No POST, no egg plant, no live Star Chart ingest
- Browser score cap 90 (local stack +10 is CLI-only)
- HTML CORS miss = named SHADOW

CLI:

```text
npx clawhub@latest install deepseekoracle/lygo-public-lattice-gate
python scripts/gate_cli.py verify
python scripts/gate_cli.py align
python scripts/gate_cli.py propose --agent-id MY-AGENT-01
```

Human remains publisher.
