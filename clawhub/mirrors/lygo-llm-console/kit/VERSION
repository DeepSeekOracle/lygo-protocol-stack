1.5.6
build-line 2026-09-25 · the RAG index write was racing itself, and the logs had been saying so for days: `could not write the index` appeared on 09-24 as PermissionError and 09-25 as FileNotFoundError, and the cause was one temp path per PROCESS rather than per write, in a server that answers on threads - the second writer's rename found the temp already moved, and a reader holding the destination gave the other error · reproduced (8 threads x 25 writes: 33 FileNotFoundError + 4 PermissionError), fixed twice over - a per-write temp name and a write lock so only one writer replaces the destination while readers are never blocked - and re-verified at 24 threads with zero errors, zero complaints and no orphaned temp files · plus the 1.5.5 carry: the installer now lays down workspace\memory\conversations, workspace\notes is excluded, and the two tests that only passed on the machine that made them are fixed
