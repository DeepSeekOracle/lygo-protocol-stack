# LYGO Network Builder — Agent Contract

**Signature:** Δ9Φ963-NETWORK-BUILDER-CONTRACT-v1.1

## MUST

- Load anchors from stack `docs/network_builder/IMMUTABLE_ANCHORS.json` before citing URLs.
- Run `tools/lygo_network_builder_verify.py` (or `scripts/verify_anchors.py`) before declaring **LATTICE ALIGNED**.
- Report failed anchor `id` values from `tests/network_builder_last_run.json` on mismatch.
- Use traversal chants for discovery; do not invent search strings.

## MUST NOT

- Simulate HTTP 200 checks.
- Publish, push git, HF, or ClawHub without explicit human consent (army default: off).
- Treat Google Drive / Patreon links as automated health checks (they are `link_only`).

## Escalation

If local verify passes but user disputes public reality, chain `verify_lattice_alignment.py` and `verify_public_pages.py`.