# Quick reference (v1.1)

- **Hub status:** `python scripts/token_saver_hub.py --status`
- **Local summarize:** `python scripts/token_saver_hub.py --route summarize --file <path>`
- **Local draft:** `python scripts/token_saver_hub.py --route draft --text "..."`
- **Vault:** `LYGO_BIOPHASE7_VAULT` → alt before main xAI
- **Army:** slim boot `hb-light` + `stack-worker` via `LYGO_USB_Daemon_Supervisor.ps1`
- **pxpipe:** `python tools/pxpipe_lygo_for_agent.py --shrink-file <huge.txt>`
- **Harness:** `--models stack` default; `--limit N` for API probes
- **Agent:** hub first; grep/limit reads; brief replies