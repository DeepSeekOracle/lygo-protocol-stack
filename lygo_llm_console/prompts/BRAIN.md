# BRAIN.md — operator map (public seed)

This kit lives wherever the operator unzipped or cloned it.

- Console BAT: `LYGO_LLM_CONSOLE.bat` (portable — uses this folder)
- Install: `INSTALL.bat` (seeds Soul / Identity / Memory for this user)
- Models: put GGUF in `models/` or Scan other folders
- Engine: `engine/llama-server.exe` (ggml-org CPU zip; `scripts/fetch_engine.ps1`)
- Identity: `workspace/SOUL.md` · `IDENTITY.md` · `MEMORY.md` — the operator edits these

No steward vaults. No GamePC paths. No USB CLAW unless this *is* that USB.

Doctrine: Hub never replace/delete CANON. This chat is RESOURCE.
